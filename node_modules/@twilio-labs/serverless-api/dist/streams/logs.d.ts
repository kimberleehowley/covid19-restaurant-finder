/// <reference types="node" />
import { Readable } from 'stream';
import { Sid, GotClient } from '../types';
import { LogsConfig } from '../types/logs';
export declare class LogsStream extends Readable {
    private environmentSid;
    private serviceSid;
    private client;
    private config;
    private _pollingFrequency;
    private _interval;
    private _viewedSids;
    constructor(environmentSid: Sid, serviceSid: Sid, client: GotClient, config: LogsConfig);
    pollingFrequency: number;
    _poll(): Promise<void>;
    _read(): void;
    _destroy(): void;
}
