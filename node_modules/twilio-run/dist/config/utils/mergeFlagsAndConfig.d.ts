import { SharedFlags } from '../../commands/shared';
import { CliInfo } from '../../commands/types';
export declare function mergeFlagsAndConfig<T extends SharedFlags & {
    [key: string]: any;
}>(config: Partial<T>, flags: T, cliInfo: CliInfo): T;
